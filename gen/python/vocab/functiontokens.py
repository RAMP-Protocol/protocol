# Code generated by protoc-gen-rampvocab. DO NOT EDIT.
#
# Source vocabulary: on ramp.v1.RESTRICTION_KIND_FUNCTION.
# The token list is authored solely in that option; these constants,
# is_registered, ALIASES and canonical derive from it and cannot drift.

from collections.abc import Mapping
from types import MappingProxyType

ALL_USES = "all"
AI_ALL = "ai-all"
AI_TRAIN = "ai-train"
AI_INPUT = "ai-input"
AI_INDEX = "ai-index"
SEARCH = "search"
CRAWL = "crawl"
TEXT_AND_DATA_MINING = "text-and-data-mining"
TTS = "tts"
COMMERCIAL = "commercial"
ADVERTISING = "advertising"
EDITORIAL = "editorial"
RESEARCH = "research"
REPRODUCE = "reproduce"
DISTRIBUTE = "distribute"
MODIFY = "modify"
DISPLAY = "display"
SYNC = "sync"
BROADCAST = "broadcast"
STREAM = "stream"
PRINT = "print"
MANUFACTURE = "manufacture"
SELL = "sell"

# ALL lists every registered token in registration order.
ALL = (
    ALL_USES,
    AI_ALL,
    AI_TRAIN,
    AI_INPUT,
    AI_INDEX,
    SEARCH,
    CRAWL,
    TEXT_AND_DATA_MINING,
    TTS,
    COMMERCIAL,
    ADVERTISING,
    EDITORIAL,
    RESEARCH,
    REPRODUCE,
    DISTRIBUTE,
    MODIFY,
    DISPLAY,
    SYNC,
    BROADCAST,
    STREAM,
    PRINT,
    MANUFACTURE,
    SELL,
)

_REGISTERED = frozenset(ALL)


def is_registered(s: str) -> bool:
    """Return True if s is a registered bare token (namespaced vendor:token values return False)."""
    return s in _REGISTERED


# ALIASES maps an accepted alias spelling to the registered token it
# canonicalises to. Authored beside the tokens; an axis without aliases
# carries an empty mapping so every axis has the same face.
ALIASES: Mapping[str, str] = MappingProxyType({
    "adapt": MODIFY,
    "copy": REPRODUCE,
    "derivative": MODIFY,
    "generative-ai": AI_INPUT,
    "scrape": CRAWL,
    "tdm": TEXT_AND_DATA_MINING,
    "train-ai": AI_TRAIN,
})


def canonical(s: str) -> str:
    """Return the registered token s is an alias of, or s unchanged (no trimming or case folding: the SDK folds first)."""
    return ALIASES.get(s, s)
