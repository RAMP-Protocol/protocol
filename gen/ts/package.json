{
  "name": "@ramp-protocol/sdk",
  "version": "1.0.0",
  "description": "RAMP protocol — generated vocabulary constants (TypeScript). Message/validation types are generated as Zod schemas from JSON Schema (added via scripts/gen-sdk-types.sh).",
  "type": "module",
  "exports": {
    "./vocab/*": "./vocab/*.ts"
  }
}
