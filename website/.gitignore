# build output
dist/
# generated types
.astro/
# generated vocabulary view — recomputed every build from the proto by
# `npm run gen:vocab` (cmd/vocabjson); never hand-edited, never committed.
src/data/vocab.json

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*


# environment variables
.env
.env.production

# macOS-specific files
.DS_Store
